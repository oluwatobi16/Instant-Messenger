To initialise the instant messenger first ensure python 3.x is installed on your device then
download all the code files. Open a terminal and navigate to the directory where all
the files are located and enter python server.py "host address" "port".
Then in a separate terminal navigate to the same directory and run 
python client.py "host address" "port"